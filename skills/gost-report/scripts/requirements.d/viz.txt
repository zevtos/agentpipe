# Тир [viz] — matplotlib-графики по ГОСТ. Активируется:
#   GOST_REPORT_EXTRAS=viz python3 scripts/ensure_env.py
# Только numpy+matplotlib (~66 МБ). scipy/pandas НЕ тащим — не нужны для
# базовых чартов и весят втрое больше (см. research/19).
# Диапазоны, а не точные пины: общий venv может уже содержать новее (mpl 3.10+);
# для байт-в-байт детерминизма зафиксируй точные версии в своём окружении.
numpy>=2.0,<3
matplotlib>=3.9,<4

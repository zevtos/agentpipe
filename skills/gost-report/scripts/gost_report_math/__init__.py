"""gost_report_math — формулы (LaTeX → OMML) как подключаемый модуль.

Namespace r.math (r.math.formula). r.formula(...) — BC-shim → r.math.formula.
Первый extracted-модуль из монолита: образец для остальных (см. research/19 §5).
Зависимость latex2mathml в default-тире, поэтому модуль auto-attach.
"""
